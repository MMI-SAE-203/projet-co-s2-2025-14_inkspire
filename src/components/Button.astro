---
export interface Props {
  variant?: "plain" | "stroke";
  text?: string;
  url?: string;
}

const { text, url, variant } = Astro.props;

const variantClass = {
  stroke: "border-neutral-600",
  plain: "border-neutral-600 bg-neutral-600",
};
---

<a
  href={url}
  class={`flex items-center justify-center w-48 px-4 py-6 gap-2.5 border-2 rounded-xl text-white font-montserrat_alt text-base transition ${variantClass[variant]}`}
  >{text}
</a>
